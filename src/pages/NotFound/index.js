import React from 'react'
import { Layout } from '../../components/Layout'

export const NotFound = () => (
	<Layout title='Página no encontrada' subtitle='Lo sentimos, no se encontró la página deseada :('>
		<h1>Esta página no existe! :(</h1>
	</Layout>
)
